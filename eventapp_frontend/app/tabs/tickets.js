import { Box, Heading } from 'native-base';

export default function TicketsScreen() {
  return (
    <Box flex={1} p={4} bg="white">
      <Heading mb={4} size="lg">Vé của tôi</Heading>
      {/* Thêm danh sách vé sau */}
    </Box>
  );
}